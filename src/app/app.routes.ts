import { Routes } from '@angular/router';
import { IconMap } from './icon-map/icon-map';

export const routes: Routes = [
  { path: '', component: IconMap },
  { path: 'icon-map', component: IconMap },
];
